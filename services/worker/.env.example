# Environment Variable Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env in the project root
# 2. Replace placeholder values with your actual credentials
# 3. NEVER commit .env file to version control
# 4. .env is gitignored - keep it that way
#
# WARNING: Exposing API keys can result in unauthorized usage and costs.
# Treat .env file contents as highly sensitive.

# ==============================================================================
# AI PROVIDER API KEYS (Required for external runner)
# ==============================================================================
#
# CRITICAL: These keys are used ONLY in the external runner (never in repo core).
# See docs/ai/PROVIDER_REGISTRY.md for approved providers and constraints.
# See config/tools/SECRETS_POLICY.md for key management requirements.
#
# All providers process DE-IDENTIFIED DATA ONLY. NEVER send PHI to external APIs.

# OpenAI API Key (GPT-4, GPT-4o) - General reasoning, summarization
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Azure OpenAI - Enterprise-grade OpenAI with regional deployment
# Get from: Azure Portal → Azure OpenAI Service
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_DEPLOYMENT=

# Anthropic API Key (Claude 3.5 Sonnet) - Structured analysis, long context
# Get from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Together AI API Key (Llama, Mixtral, Qwen, DeepSeek) - Cost-effective bulk runs
# Get from: https://api.together.xyz/settings/api-keys
TOGETHER_API_KEY=

# Grok / xAI API Key - Experimental hypothesis ideation
# Get from: https://console.x.ai/ (or xAI developer portal)
XAI_API_KEY=

# ==============================================================================
# GITHUB INTEGRATION (Optional - for PR automation)
# ==============================================================================

# GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# Required scopes: repo, workflow (if using Actions)
GITHUB_TOKEN=

# ==============================================================================
# MONITORING & OBSERVABILITY (Optional)
# ==============================================================================

# Datadog API Key - Optional
# Get from: https://app.datadoghq.com/organization-settings/api-keys
DATADOG_API_KEY=

# Sentry DSN - Optional
# Get from: https://sentry.io/settings/[org]/projects/[project]/keys/
SENTRY_DSN=

# ==============================================================================
# AUTHENTICATION & WORKSPACE SETTINGS
# ==============================================================================

# Authentication mode:
# - 0 = Owner bypass mode (no auth required for local dev, auto-creates owner user)
# - 1 = Authentication required (JWT must be provided for all protected endpoints)
AUTH_REQUIRED=0

# Owner email for local development (used when AUTH_REQUIRED=0)
OWNER_EMAIL=owner@local

# JWT secret key (required when AUTH_REQUIRED=1; defaults to dev-only secret if AUTH_REQUIRED=0)
# SECURITY WARNING: Generate a secure random key for production!
# Example: openssl rand -hex 32
JWT_SECRET=dev-secret-only-if-auth-not-required

# JWT token expiration time in minutes (default: 1440 = 24 hours)
JWT_EXPIRES_MIN=1440

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Database connection URL (optional; defaults to local SQLite at .tmp/ros_auth.db)
# Examples:
#   SQLite in-memory: sqlite:///:memory:
#   PostgreSQL: postgresql://user:password@localhost:5432/ros_platform
#   MySQL: mysql+pymysql://user:password@localhost:3306/ros_platform
# DATABASE_URL=

# Alternative namespaced database URL (only used if DATABASE_URL not set)
# ROS_DATABASE_URL=

# ==============================================================================
# API RUNTIME CONFIGURATION
# ==============================================================================

# CORS allowed origins (comma-separated list; default: * for development)
# Examples:
#   Development (wildcard): *
#   Production (explicit): https://app.example.com,https://admin.example.com
# SECURITY: When set to "*", allow_credentials is automatically disabled
CORS_ALLOWED_ORIGINS=*

# ==============================================================================
# PROJECT METADATA (Not secrets, but useful for env context)
# ==============================================================================

# Project identifier (used in logging/monitoring)
PROJECT_NAME=research-operating-system

# Environment: dev, test, or prod
ENVIRONMENT=dev

# ==============================================================================
# USAGE NOTES
# ==============================================================================
#
# - All secrets MUST be set as environment variables (never hardcode in code)
# - For local development: Copy this to .env and load with python-dotenv
# - For n8n runner: Set these as environment variables on n8n host
# - For CI/CD: Store in GitHub Secrets (Settings → Secrets and variables)
#
# GOVERNANCE AND COMPLIANCE:
# - See config/tools/SECRETS_POLICY.md for complete key management policy
# - See docs/ai/PROVIDER_REGISTRY.md for approved providers and data constraints
# - See docs/ai/MODEL_ROUTING_POLICY.md for task-to-provider routing rules
# - See docs/n8n/README.md for n8n credential management
# - See docs/handoffs/README.md for runner security requirements
#
# PHI PROTECTION:
# - NEVER send PHI to external AI providers (de-identified data only)
# - Pre-scan all inputs with PHI detection before external calls
# - Post-scan all AI outputs before committing to repo
#
# ==============================================================================
